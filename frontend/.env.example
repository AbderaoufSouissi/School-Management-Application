# Note: The frontend Dockerfile uses build args (API_URL) passed from docker-compose.
# However, if you use a .env file for local development or other tools, here is an example.
API_URL=http://localhost:8080/api
